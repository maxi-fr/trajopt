@AGENTS.md

## Navigating the code

The `ty` language server answers symbol questions faster than grep: `workspaceSymbol` and `documentSymbol` to locate, `goToDefinition` and `findReferences` to trace, `hover` for an inferred type, `incomingCalls` before changing a signature.
